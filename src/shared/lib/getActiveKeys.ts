import { ConfigText } from "@/features/ConfigureText/model";
import { KeyConfig } from "../types/KeyConfig";

export function getActiveKeys(keyList: KeyConfig[], configuration: ConfigText){
    for(let config in configuration){
        for(let key of keyList){
            
        }
    }
}